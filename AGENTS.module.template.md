<!--
模块 AGENTS 模板 v1.0 · 维护仓库：vibe-coding
用法：多模块仓库中，复制到各模块目录（如 <repo>/<模块>/AGENTS.md）→ 填充【】占位。
原则：根 AGENTS.md 管"流程与公共规范"（文档/计划/Tag/行为准则），本文件只写
"换个模块就会变"的内容，公共规范不在此重复，只链接回根文件。
-->

# 【模块名】模块约定

> 公共规范（文档与计划 / 更新日志 / Tag / 行为准则）见仓库根 `AGENTS.md`，本文件只写本模块特有内容。

## 模块概览

【这个模块是什么、鉴权/接入模型、是否零依赖。一两句。】

## 目录结构

【本模块内的包划分，一行一个带注释。】

## 接口约定

【只有这个模块知道的协议事实：域名、公共参数、错误码、签名方式。以实测为准的写明实测日期。】

## 常用命令

```bash
【本模块单独构建 / 测试 / 跑实测的方式，含需要的环境变量名】
```

## 模块特有规矩

【本模块的坑与红线：如反爬参数在哪配、哪些旧接口已废弃、与哪些分支/方案不混用、文档索引在哪。】
