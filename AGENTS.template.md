<!--
公共 AGENTS 模板 v1.0 · 维护仓库：vibe-coding
用法：复制本文件为新项目根目录的 AGENTS.md → 填充所有【】占位符 → 保留文件头注释并注明所用版本。
公共规范（文档与计划 / 更新日志 / Tag）修改时只改本模板；项目内文件如需跟进，按文件头版本号对比升级。
多模块仓库：另复制 AGENTS.module.template.md 到各模块目录。
-->

# AGENTS.md

## 项目概览

【一两句话：项目是什么、技术栈、模块名。写"换个项目就失效"的信息，通用信息不要写在这里。】

## 目录结构

【关键目录树，一行一个目录带注释。只列需要解释的，`ls` 能看出来的不用写。】

## 常用命令

```bash
【构建】
【测试】
【本地运行 / 联调】
```

## 项目约定

【项目特有的架构模式、错误处理、协议约定。"换个项目就变"的写这里。】

- **凭据红线**（通用，必留）：密钥 / cookie / token 只走环境变量或本地配置文件，绝不写进代码、示例默认值和文档。
- **完成标准**（通用，必留）：改动以 build / vet / test 全过为完成，不含糊。

## 文档与计划（公共规范，勿改）

- `docs/plans/`：开发 / 重构计划。**计划先行**——多步任务先落计划再动手，计划不落盘不动工；每项任务带验收标准与 checkbox，执行中更新勾选，完成后文首标注完成日期。
- `docs/reviews/`：代码审查报告，文件名 `YYYY-MM-DD-<范围>.md`。固定六节：范围 / 方法 / 结论摘要 / 发现 / 修复计划 / 勘误记录。发现按 P0（正确性·安全）/ P1（应尽快）/ P2（建议）分级，每条 = 位置 + 依据 + 处置；已修标 ☑；写错的发现进"勘误记录"撤销，不删除。
- `TODO.md`：根目录任务索引，只做勾选清单，详情链接到 docs/plans 与 reviews，不重复正文。
- 接口参考等深入文档放 `docs/<模块或主题>/`，不放根目录。

## 更新日志（公共规范，勿改）

- 对外发布的仓库根放 `CHANGELOG.md`，Keep a Changelog 风格：

  ```markdown
  ## [Unreleased]

  ## [0.4.0] - 2026-09-12
  ### 新增
  ### 修复
  ### 变更 / 移除
  ```

- 条目与 tag **一一对应：先补条目，后打 tag**；未发版改动先进 `## [Unreleased]`。

## Tag 规范（公共规范，勿改）

- `vX.Y.Z`（semver）：不兼容变更升主位，新功能升次位，修复升第三位。
- 发 tag 前置：build / vet / test 全过，且 CHANGELOG 对应条目已就绪。
- 多模块仓库变体：tag 带模块前缀，如 `模块名/v0.2.0`。

## 行为准则（公共规范，勿改）

以下准则偏向谨慎而非速度，目的是减少常见编码失误。

### 1. 先想清楚再动手
不要想当然。不要掩盖困惑。主动暴露取舍。
动手之前：
- 明确说出你的假设，不确定就问
- 如果有多种理解，列出来让用户选，不要自己悄悄选一个
- 如果存在更简单的方案，说出来。该反驳就反驳
- 遇到不清楚的地方，停下来。指出哪里不明白，然后问

### 2. 简单优先
用最少的代码解决问题。不要写猜测性的代码。
- 不加没要求的功能
- 一次性使用的代码不做抽象
- 没要求的"灵活性"或"可配置性"不加
- 不可能发生的场景不做错误处理
- 如果写了 200 行但 50 行就能搞定，重写
- 问自己："资深工程师会觉得这太复杂了吗？" 如果是，简化

### 3. 精准修改
只动该动的。只清理自己弄乱的。
编辑已有代码时：
- 不要"顺手改进"旁边的代码、注释或格式
- 不要重构没坏的东西
- 匹配已有风格，即使你习惯不同
- 发现无关的死代码，提一句就行，别删
你的改动产生的孤立代码：
- 删除你的改动导致不再使用的 import/变量/函数
- 不要删除之前就存在的死代码，除非被要求
检验标准：每一行改动都应该能追溯到用户的需求。

### 4. 目标驱动
定义成功标准，循环直到验证通过。
把任务转化为可验证的目标：
- "加校验" → "为无效输入写测试，然后让测试通过"
- "修 bug" → "写一个能复现的测试，然后让它通过"
- "重构 X" → "确保重构前后测试都能通过"
多步骤任务，简要列个计划：
1. [步骤] → 验证：[检查点]
2. [步骤] → 验证：[检查点]
3. [步骤] → 验证：[检查点]
